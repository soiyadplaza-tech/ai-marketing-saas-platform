# Contributing

## Development

```bash
npm install
npm run typecheck
npm run lint
npm run build
```

Keep changes focused and production-safe. Do not commit credentials or generated deployment secrets.

## Pull requests

Every PR should explain:
- what changed
- why it changed
- how it was tested
- any required environment variables or migration steps

CI must pass before merge.
